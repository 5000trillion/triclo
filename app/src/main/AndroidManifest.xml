<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.t_robop.triclo.triclo">

    <uses-sdk tools:overrideLibrary="org.opencv" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".ClRegistActivity" />
        <activity android:name=".TestDbView" />
        <activity android:name=".CodePickActivity" />
        <activity android:name=".ImgCodeTopsActivity" />
        <activity android:name=".Tab1_Activity" />
        <activity
            android:name=".DebugActivity"
            android:label="DebugActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestDbView" />
        <activity android:name=".MaskActivity" />
        <activity android:name=".ImgTopsAllActivity" />
        <activity android:name=".ImgTopsOuterActivity" />
        <activity android:name=".ImgTopsInnerActivity" />
        <activity android:name=".ImgCodeBottomsActivity" />
        <activity android:name=".ImgCodeAccessoryctivity"></activity>
    </application>

</manifest>